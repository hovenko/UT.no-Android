package no.ut.trip;

import no.nrk.listings.facet.FacetSet;

public class SubjectsRepository {
    public FacetSet getSubjects() {
	FacetSet subjects = new FacetSet();

	// Resource resource = new DefaultResource(url, label);
	// QueryResource query = new ListingResource(resource, value);
	// FacetResource facetCottage = new FacetResource(query, found)

	return subjects;
    }
}
